# QuadTree-VectorDB // Spatial Grid Partitioning Index

A 2D vector space partitioner using native memory quadtree quadrant expansions to accelerate target spatial query lookups without using brute-force matrix iterations.

## Run App
```bash
python spatial_rag_app.py
